selectTipBoardList = SELECT * FROM ( SELECT ROWNUM "RNUM", V.* FROM( SELECT V.*, M.NAME, M.MEMBER_ROLE FROM T_POST V LEFT OUTER JOIN MEMBER M ON V.USER_KEY = M.KEY ORDER BY V.KEY DESC) V ) WHERE RNUM BETWEEN ? AND ?
selectTipBoardCount = select count(*) as "cnt" from T_POST